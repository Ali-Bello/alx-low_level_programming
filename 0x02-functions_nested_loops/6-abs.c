#include "main.h"
/**
  *_abs - Gives the absolute value of an integer
  *@n: the number to work with
  *
  *Return: the absolute value
  */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
